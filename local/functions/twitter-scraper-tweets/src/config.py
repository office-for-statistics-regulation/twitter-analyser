# Tweepy Config
maxTweets = 10000
tweetsPerQry = 100  # this is the max the API permits

# Twitter Searches
topic_or_mentions = 'Environment'
search_query = '(environment AND statistics) OR (environment AND data) OR ("renewable energy" AND ' \
					   'statistics) OR ("renewable energy" AND data) OR ("climate change" AND statistics) OR ' \
					   '("climate change" AND data) OR (carbon AND statistics) OR (climate AND data) OR ' \
					   '(farming AND data) OR (farming AND statistics) OR ("greenhouse gas" AND data)'

# topic_or_mentions = 'OSR'
# search_query = '@UKStatsAuth OR @StatsRegulation -filter:retweets'
